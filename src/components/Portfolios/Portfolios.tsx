import React from 'react';
import styles from "components/Portfolios/Portfolios.module.css"

const Portfolios: React.FC = () => {
    return (
        <div className={styles.portfolios}>
            <p>Portfolios</p>
        </div>
    )
}

export default Portfolios;